# Introduce to run project

## Install denpendencies package in `ClientApp`

```bash
cd ClientApp
npm install
```

## Run project

> Note: run command only you stand in root folder (`Folder has Startup.cs`)

```bash
dotnet run
```

